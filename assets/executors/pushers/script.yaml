apiVersion: g2a-cli/v2.0
kind: Pusher
name: script
schema:
  type: object
  required:
    - sh
  properties:
    sh:
      type: string
script: |
  exec('sh', [ '-c', 'export PUSH=true\n' + input.spec.sh ])
