apiVersion: g2a-cli/v2.0
kind: Pusher
name: artifactory
schema:
  type: object
  required:
    - source
    - target
  properties:
    source:
      type: string
      minLength: 1
    target:
      type: string
      minLength: 1
script: |
  abort("not implemented")
